generator client { provider = "prisma-client-js" }
datasource db { provider = "postgresql" url = env("DATABASE_URL") }
model Lead { id String @id @default(cuid()) type String name String company String? email String phone String? city String? message String? product String? createdAt DateTime @default(now()) @@index([createdAt]) }