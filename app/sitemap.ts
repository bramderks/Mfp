import type {MetadataRoute} from "next";import {products} from "@/lib/products";export default function sitemap():MetadataRoute.Sitemap{const base="https://mfp.iselto.nl";return ["/","/producten","/oplossingen","/diensten","/downloads","/offerte","/contact","/zoeken","/vergelijker"].map(path=>({url:base+path,lastModified:new Date()})).concat(products.map(p=>({url:base+"/producten/"+p.slug,lastModified:new Date()})))} 