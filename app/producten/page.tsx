import Link from "next/link";import Image from "next/image";import {products} from "@/lib/products";export const metadata={title:"Producten"};export default function Products(){return <main className="container" style={{paddingTop:70}}><div className="eyebrow">PRODUCTCATALOGUS</div><h1 style={{fontSize:52,color:"var(--navy)",margin:"10px 0"}}>Multifunctionals & printers</h1><p style={{fontSize:18,color:"#5d6874",maxWidth:760}}>Bekijk onze MFP-oplossingen. Op iedere productpagina vindt u de belangrijkste specificaties en kunt u direct een offerte aanvragen.</p><div style={{display:"grid",gridTemplateColumns:"repeat(2,1fr)",gap:24,marginTop:45,marginBottom:80}}>{products.map(p=><Link className="card" href={"/producten/"+p.slug} key={p.slug} style={{textDecoration:"none",color:"inherit",display:"grid",gridTemplateColumns:"1fr 1fr"}}><div style={{position:"relative",minHeight:300}}><Image src={p.image} alt={p.name} fill style={{objectFit:"cover"}} sizes="50vw"/></div><div style={{padding:30}}><div className="eyebrow">{p.category}</div><h2 style={{color:"var(--navy)"}}>{p.name}</h2><p style={{color:"#5d6874",lineHeight:1.6}}>{p.description}</p><p><b>{p.speed}</b> · {p.format} · {p.colour?"Kleur":"Zwart-wit"}</p><span style={{color:"var(--blue)",fontWeight:700}}>Bekijk specificaties →</span></div></Link>)}</div></main>}