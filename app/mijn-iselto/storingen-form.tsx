"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export function TicketForm(){const router=useRouter();const [subject,setSubject]=useState("");const [description,setDescription]=useState("");const [priority,setPriority]=useState("normal");const [machineId,setMachineId]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState("");
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/portal/tickets",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({subject,description,priority,machineId:machineId||null})});const d=await r.json();if(!r.ok)throw new Error(d.error);router.push("/mijn-iselto/storingen");router.refresh()}catch(e){setError(e instanceof Error?e.message:"Melding kon niet worden verstuurd")}finally{setBusy(false)}}
return <form className="portal-form" onSubmit={submit}><div className="portal-form-grid"><div className="portal-field full"><label>Onderwerp</label><input className="portal-input" required value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Wat is er aan de hand?"/></div><div className="portal-field"><label>Prioriteit</label><select className="portal-select" value={priority} onChange={e=>setPriority(e.target.value)}><option value="low">Laag</option><option value="normal">Normaal</option><option value="high">Hoog</option><option value="urgent">Urgent</option></select></div><div className="portal-field"><label>Assetnummer (optioneel)</label><input className="portal-input" value={machineId} onChange={e=>setMachineId(e.target.value)} placeholder="Assetnummer"/></div><div className="portal-field full"><label>Omschrijving</label><textarea className="portal-textarea" required value={description} onChange={e=>setDescription(e.target.value)} placeholder="Omschrijf de storing zo duidelijk mogelijk." /></div></div>{error&&<p className="portal-error">{error}</p>}<div className="portal-actions" style={{marginTop:20}}><button className="portal-btn primary" disabled={busy}>{busy?"Versturen…":"Storing melden"}</button></div></form>}
