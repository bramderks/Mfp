import LeadForm from "@/components/LeadForm";export default async function Quote({searchParams}:{searchParams:Promise<{product?:string}>}){const q=await searchParams;return <main className="container" style={{paddingTop:70}}><div className="eyebrow">OFFERTE & ADVIES</div><h1 style={{fontSize:52,color:"var(--navy)"}}>Vraag een offerte aan</h1><p style={{fontSize:19,color:"#5d6874",maxWidth:760}}>Vertel ons over uw organisatie en uw printbehoefte. We nemen persoonlijk contact op.</p><LeadForm type="quote" product={q.product}/></main>}