"use client";import {useState} from "react";export default function LeadForm({type,product}:{type:"quote"|"contact";product?:string}){const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,type,product})});setLoading(false);if(r.ok)setDone(true)}if(done)return <div className="card" style={{padding:35,maxWidth:720,margin:"40px 0 100px"}}><h2 style={{color:"var(--navy)"}}>Bedankt voor uw aanvraag</h2><p style={{color:"#5d6874"}}>We hebben uw gegevens ontvangen en nemen zo snel mogelijk contact met u op.</p></div>;return <form onSubmit={submit} style={{maxWidth:760,margin:"40px 0 100px",display:"grid",gap:16}}><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:16}}><input className="input" name="name" placeholder="Naam *" required/><input className="input" name="company" placeholder="Bedrijf"/></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:16}}><input className="input" name="email" type="email" placeholder="E-mail *" required/><input className="input" name="phone" placeholder="Telefoon"/></div><input className="input" name="city" placeholder="Plaats"/><textarea className="input" name="message" placeholder={type==="quote"?"Vertel iets over uw huidige situatie en wensen":"Uw vraag"} rows={7}/><button className="btn btn-primary" disabled={loading}>{loading?"Verzenden…":"Aanvraag versturen"}</button></form>}